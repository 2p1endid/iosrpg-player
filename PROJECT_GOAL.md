# iOS RPG Player 项目总目标

## 1. 项目愿景

开发一款运行于 iPhone 和 iPad 的本地 RPG Maker 游戏播放器。用户导入自己合法拥有的 RPG Maker 游戏文件后，可以在移动设备上管理、启动和游玩游戏，并使用触屏虚拟按键和本地存档。

产品功能可以参考 ArkRPG 的使用闭环，但必须采用独立的产品名称、界面、代码和资源，不复制其商标、图标、截图或专有实现。

## 2. 总体目标

最终产品应形成以下完整链路：

1. 用户从“文件”App 选择游戏文件夹或压缩包。
2. 应用安全复制或解压游戏到自身沙盒。
3. 应用识别 RPG Maker 引擎版本并验证目录完整性。
4. 用户在游戏库中查看、重命名、启动和删除游戏。
5. 播放器加载游戏资源并稳定运行。
6. 用户通过触屏虚拟按键、触摸、鼠标模拟或外接手柄操作游戏。
7. 游戏存档在退出和重启应用后仍然保留。
8. 后续版本支持存档备份、跨设备同步、翻译文件和旧版 RGSS 游戏。

## 3. 分阶段范围

### 阶段 A：MV/MZ Web 运行环境（当前阶段）

目标是制作一个可在真实 iOS 工具链中构建和安装的技术验证 App，并尽可能产出 IPA。

必须实现：

- SwiftUI 应用外壳。
- `WKWebView` 游戏运行容器。
- 加载 App 内置的 HTML/JavaScript 测试游戏。
- 从本地目录加载 RPG Maker MV/MZ 风格的 `index.html` 与关联资源。
- 基于文件标志识别 MV 与 MZ 项目。
- 屏幕方向键、确认键和取消键。
- Swift 到 JavaScript 的键盘事件桥接。
- JavaScript 错误与资源加载错误的可读提示。
- 基础本地持久化验证。
- 自动化测试覆盖游戏识别、路径安全和 MIME 类型判断。

阶段 A 验收条件：

- 内置测试游戏能在浏览器或等效 Web 环境中运行。
- 核心自动化测试全部通过。
- Xcode 工程结构完整。
- 在可用的 macOS/Xcode 签名环境中成功构建归档并导出 IPA；如果当前机器没有 Apple 工具链，则必须留下可复现的构建脚本并明确记录阻塞原因，不能伪造 IPA。

### 阶段 B：本地游戏导入与游戏库

- 文件夹、ZIP 导入。
- 安全解压，防止路径穿越和解压炸弹。
- 游戏根目录自动定位。
- 游戏元数据、封面和游戏库。
- 导入进度、中断恢复和明确错误提示。
- 重命名、删除和存储空间统计。

### 阶段 C：移动端控制与兼容性

- 每个游戏独立的虚拟按键布局。
- 按键拖动、缩放、透明度和映射。
- 横屏和竖屏布局。
- 鼠标点击、右键和滚轮模拟。
- iOS 外接手柄。
- MV/MZ 插件兼容模式和安全模式。

### 阶段 D：存档与增强功能

- MV/MZ 本地存档统一管理。
- 存档导入、导出、备份和恢复。
- iCloud/CloudKit 同步与冲突处理。
- MTool JSON 翻译文件。
- RAR、7z 和密码压缩包。
- 不兼容视频格式的检测与可选转换。

### 阶段 E：XP/VX/VX Ace RGSS 环境

这是独立的原生引擎工程，不纳入首版 MVP。

研究方向包括：

- RGSS1、RGSS2、RGSS3。
- Ruby 运行时。
- 原生图形、音频、输入和归档兼容层。
- RTP 管理。
- iOS 沙盒、生命周期和内存适配。
- 开源引擎许可证与 App Store 分发合规性。

在决定采用 GPL 等强 copyleft 组件前，必须完成正式许可证评估。

## 4. 非目标

当前阶段不做：

- 内置或分发任何第三方商业游戏。
- 游戏下载商店或盗版资源搜索。
- ArkRPG 品牌、界面或二进制逆向复制。
- XP/VX/VX Ace 完整运行环境。
- 云存档、订阅和复杂商业化。
- 作弊、内存修改、宏和连发。

## 5. 技术原则

- iOS 主应用使用 Swift、SwiftUI、WebKit。
- 运行环境优先采用系统公开 API。
- 游戏文件严格限制在应用沙盒和单游戏目录中。
- 路径解析必须拒绝目录穿越、绝对路径和越界访问。
- 每个游戏拥有独立 ID、配置、控制布局和存档空间。
- 游戏引擎通过统一适配器接口接入，避免将 UI 与某个引擎写死。
- 新增核心行为遵循测试优先：先写失败测试，再写最小实现，再验证全套测试。
- 任何 IPA、构建成功或运行成功声明都必须有真实工具输出支持。

## 6. App Store 与法律边界

- App 不包含游戏本体，用户只能导入合法拥有的内容。
- 不向游戏脚本开放联系人、相册、定位等原生权限。
- 拦截不受控外链、下载、弹窗和原生 API 调用。
- 使用独立品牌，并注明与 RPG Maker 官方不存在从属关系。
- 发布前重新核查最新 App Review Guidelines，重点关注 2.5.2 与 4.7。
- 为 App Review 准备自制、可合法分发的测试游戏和完整审核说明。

## 7. 当前交付物

本轮应交付：

1. 本文件 `PROJECT_GOAL.md`。
2. MV/MZ Web 运行技术验证代码。
3. 可打开的 iOS Xcode 工程或可确定性生成 Xcode 工程的项目描述。
4. 内置测试游戏。
5. 自动化测试及真实测试结果。
6. IPA；若受 Windows 缺少 Xcode/macOS/签名证书所限，则交付构建脚本、工程文件和明确的未完成原因。
